/**
 * Created by Тимакс on 29.11.2015.
 */
public class Triangle {
    static double area;
    int b=2,h=3;

    public static void main(String[] args) {
        double p,b,h;
        if(area == 0){
            p=0.2;
            b=3;
            h=4;
        }
//        area = p*b*h; //compilation error

        Boolean aBoolean = new Boolean(Boolean.parseBoolean("true"));
        Boolean bBoolean = new Boolean(null);
        System.out.println(aBoolean+" "+bBoolean);

    }
}
